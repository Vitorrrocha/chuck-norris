import React from 'react';

import './style.css';

class Button extends React.Component {
    render() {
        return(
            <button>

            </button>
        )
    }
}

export default Button;